# DV3 — Voice Companion with Emotional Visualizer

A voice-activated home companion that listens for a wake word, conducts natural conversation via the Gemini Live API, and drives a fullscreen ambient visualizer that responds to emotional context extracted from the conversation.

**Platform**: Ubuntu/WSL2 on Windows NUC
**Status**: Active Development — voice pipeline operational, web editor shipped, animation library migration in progress

---

## How It Works

```
[Wake Word] → [Gemini Live API] → [Emotion Parser] → [Visualizer]
                    |
                [Tools: Spotify, Timers, System]
```

1. Wake word detection (OpenWakeWord, local CPU) gates the Gemini Live session
2. Gemini Live handles VAD, STT, LLM reasoning, and TTS in a single WebSocket session
3. Every Gemini text response begins with a bracketed emotion tag: `[curious] That's interesting...`
4. The emotion parser extracts the tag, triggering a crossfade to the matching animation
5. Contextual triggers (music genre, weather terms) override emotion animations
6. A Pygame visualizer plays animated WebP files fullscreen with a radial black gradient mask

---

## Project Structure

```
dv3/
├── main.py                     # Entry point — asyncio event loop + Pygame render thread
├── config/
│   ├── settings.yaml           # All runtime config (wake word path, model, gradient, etc.)
│   └── emotion_map.yaml        # Emotion/state/contextual trigger definitions
├── core/
│   ├── emotion_parser.py       # Tag extraction → keyword fallback → neutral default
│   ├── wake_word.py            # OpenWakeWord gate (sounddevice, WSL2-safe)
│   ├── visualizer_ws.py        # WebSocket server — broadcasts emotion events to web clients
│   └── voice_pipeline/
│       ├── base.py             # Abstract interface (swap backends without touching visualizer)
│       ├── gemini_live.py      # Gemini Live API implementation
│       └── modular.py          # Stub for future alternative backend
├── visualizer/
│   ├── display.py              # Pygame window, resolution-agnostic scaling
│   ├── animation_engine.py     # WebP/GIF playback, crossfade, frame cache
│   ├── gradient_overlay.py     # Radial black gradient mask
│   └── emotion_map.py          # Manifest-based animation resolver
├── tools/
│   ├── tool_dispatcher.py      # Routes Gemini tool calls to handlers
│   ├── spotify_tool.py         # Spotify play/pause/skip/volume/search
│   ├── timer_tool.py           # Concurrent timers with TTS alerts
│   └── system_tools.py         # Time/date
├── editor-web/                 # PRIMARY animation editor (browser-based)
├── editor/                     # LEGACY — headless batch conversion only (converter.py)
├── data/
│   └── animations/
│       ├── manifest.json       # Asset index — generated by editor on save
│       ├── inbox/              # Raw imported files awaiting tagging
│       └── library/            # Tagged and baked animations, ready for runtime use
├── tests/                      # pytest suites for all components
└── docs/plans/                 # Implementation plans
```

---

## Setup

### Requirements

- Python 3.12+
- Node.js 18+ (for web editor)
- PulseAudio or PipeWire bridge (WSL2 audio)
- Wake word model: `*.onnx` file configured in `settings.yaml`

### Python Companion

```bash
cd ~/projects/dv3
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Configure
cp .env.example .env   # add your Gemini API keys and Spotify credentials
# Edit config/settings.yaml — set wake_word.model_path

# Run
python main.py
python main.py --debug      # verbose logging
python main.py --windowed   # no fullscreen
```

### Web Editor

```bash
cd editor-web
npm install
npm run dev     # → http://localhost:5173
```

### Tests

```bash
source venv/bin/activate
pytest tests/                               # all tests
pytest tests/test_emotion_parser.py -v     # specific suite
cd editor-web && npm run typecheck          # TypeScript check
cd editor-web && npm run lint               # ESLint
```

---

## Web Editor (DV3 Editor)

The browser-based animation editor is the primary tool for building the DV3 animation library. Located at `editor-web/`, runs at `http://localhost:5173`.

**Workflow:**

1. **Import** — Use the Import button to load GIF, WebP, PNG, or JPG files into the Inbox
2. **Edit** — Apply non-destructive adjustments (brightness, contrast, hue, flip, crop, speed, reverse, etc.)
3. **Tag** — In the Tag Panel, select emotions, states, custom tags, theme, title, and notes
4. **Save** — Bakes edits via FFmpeg WASM and writes the final WebP + manifest entry to `data/animations/library/`
5. **Library** — Switch to the Library tab to re-edit, duplicate, or remove saved assets
6. **Export** — Download a ZIP of all library assets + `manifest.json` for backup or transfer

See [editor-web/README.md](editor-web/README.md) for full documentation.

---

## Configuration

All runtime settings are in `config/settings.yaml`. No sensitive values belong here — those go in `.env`.

Key settings:
- `wake_word.model_path` — path to the `.onnx` wake word model
- `voice.backend` — `gemini_live` (only active backend)
- `voice.model` — Gemini model ID
- `visualizer.fullscreen` / `target_fps` / `animation_height_pct`
- `visualizer.gradient.opacity` / `size`
- `animations.base_dir` — root of the animation library (`data/animations`)
- `visualizer_ws_port` — WebSocket server port (default 8765)

Secrets in `.env`:
- `GEMINI_API_KEY` / `GEMINI_API_KEY_FALLBACK`
- `SPOTIPY_CLIENT_ID` / `SPOTIPY_CLIENT_SECRET` / `SPOTIPY_REDIRECT_URI`

---

## Emotion System

Emotion detection runs a cascade:

1. **Contextual triggers** — match user speech against patterns in `emotion_map.yaml` (music genres, weather, etc.)
2. **Regex tag** — extract `[emotion]` from first 30 tokens of Gemini's text response
3. **Keyword fallback** — scan full response for sentiment keywords
4. **Default** — `neutral`

Contextual triggers override emotion triggers. Priority: contextual > emotion > keyword > neutral.

All emotion/state/contextual definitions live in `config/emotion_map.yaml`.

---

## Animation Library

Animations are managed via the web editor and stored as:

- `data/animations/library/` — flat directory of baked WebP files
- `data/animations/manifest.json` — index with full metadata (emotions, states, tags, theme)

The `EmotionMapper` class reads `manifest.json` at startup and resolves emotion queries against it, with fallback chains defined in `emotion_map.yaml`.

---

## Tools

| Tool | Trigger | Notes |
|------|---------|-------|
| Spotify | `play [song]`, `pause`, `skip`, etc. | OAuth via spotipy, credentials in `.env` |
| Timer | `set a 10 minute timer` | Concurrent timers, TTS alert on completion |
| Time/Date | `what time is it` | Native Python, no API |
| Web Search | Built into Gemini Live | `google_search` grounding, no external API |

---

## Task Tracking

This project uses **BEADS** (`bd`) for all task tracking. See [AGENTS.md](AGENTS.md).

```bash
bd ready          # What's available to work on
bd list           # All open issues
bd show <id>      # Issue details
```

---

## What's Built

- [x] Voice pipeline — Gemini Live API, wake word gate, auto-reconnect, API key fallback
- [x] Emotion detection cascade — tag extraction, keyword fallback, contextual triggers
- [x] Pygame visualizer — fullscreen, radial gradient, crossfade, frame cache, 60fps
- [x] Tool dispatch — Spotify, timers, system info
- [x] WebSocket server — broadcasts emotion/state events to web clients
- [x] Web editor — inbox/library workflow, non-destructive edits, FFmpeg WASM bake, manifest
- [x] Manifest-based animation routing
- [x] Comprehensive test suite

## What's Still To Build

- [ ] **Animation library migration** — run `scripts/migrate_to_inbox.py` to move assets from old folder structure (`emotions/`, `contextual/`, `states/`) into inbox for re-tagging; commit cleanup
- [ ] **Event tags** (dv-4vw) — additional tag types for finer-grained animation routing
- [ ] **Update tags/triggers** (dv-1tv) — refresh `emotion_map.yaml` to match current animation library
- [ ] **Paint feature** (dv-m48) — in-editor drawing/paint tool
- [ ] **Image save error fix** (dv-xst, dv-3lb) — debug save errors reported by user
- [ ] **UX improvements** (dv-5ew) — general UX polish pass
- [ ] **Delete in inbox/library** (dv-67b) — add delete action to gallery items
- [ ] **Popout notification label** (dv-p12) — fix notification header to say "DV3 EDITOR"
- [ ] **Fix remove** (dv-n9a) — fix remove action bug
- [ ] Smart bulb control (TAPO/Govee) — v1.5
- [ ] Multiple animation layers — v1.5
- [ ] Admin web panel — v1.5
- [ ] Light theme variant — v1.5 (asset model already supports `theme: dark | light | both`)
